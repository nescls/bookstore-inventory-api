# Project instructions

Use the project-local Matt Pocock skills in `.agents/skills/`.
Workflow: `grill-with-docs → to-spec → to-tickets → implement`.
Read the named skill’s SKILL.md at each stage and its supporting skills as directed.
Capture settled domain terms during the interview; resolve open decisions before
publishing the spec and tickets or starting application implementation.

Read `docs/project-brief.md` for assessment requirements, confirmed user choices,
and remaining decisions. Treat it as intake notes until the spec is agreed.

## Agent skills

### Issue tracker

Specs and tickets live in GitHub Issues for `nescls/bookstore-inventory-api`.
See `docs/agents/issue-tracker.md`.

### Domain docs

Single context: root `CONTEXT.md` and `docs/adr/`.
See `docs/agents/domain.md`.
